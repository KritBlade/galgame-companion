{
  "name": "galgame-companion",
  "version": "0.5.16",
  "private": true,
  "description": "Companion Tavern-Helper script for bigmalove/galgame — i18n overlay, custom toolbar buttons, MVU stat_data HUD, and the mvu-helper image seam. Rides on galgame untouched.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "serve": "node dev-server.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vitest": "^2.1.0"
  }
}
